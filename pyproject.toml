[project]
name = "rs485-reader"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "minimalmodbus>=2.1.1",
    "pyserial>=3.5",
    "pyqtgraph>=0.13",
    "pyside6>=6.5",
]

[tool.setuptools]
packages = ["gui", "gui.widgets"]
py-modules = [
    "main",
    "modbus_reader",
    "parsivel2_parser",
    "port_config",
    "read_sensor",
]
